/** * Generates a random past date within the last two years. * @returns {string} A string representing a date in the format 'YYYY-MM-DD' */export const generateRandomPastDate = (): string => {	const now = new Date()	// Calculate a past date by subtracting a random number of milliseconds	// 2 years * 365 days * 24 hours * 60 minutes * 60 seconds * 1000 milliseconds	const past = new Date(		now.getTime() - Math.random() * 2 * 365 * 24 * 60 * 60 * 1000	)	// Convert the date to ISO format and get only the date	return past.toISOString().split('T')[0]}