// Utility function to retrieve the Pexels API key based on the current environmentexport const getPexelsApiKey = (): string => {	// Check if running in a browser-like environment with Vite's `import.meta.env`	if (		typeof import.meta !== 'undefined' &&		import.meta.env?.VITE_PEXELS_API_KEY	) {		return import.meta.env.VITE_PEXELS_API_KEY	}	// In test environments, return a mock/test key	if (process.env.NODE_ENV === 'test') {		return 'uWUmcWyhZt1bTdHfNT4Lat38vgNlSgsYYIYY9ns67O6Mb0wWP3HQ36Kj'	}	// No API key was found	throw new Error('VITE_PEXELS_API_KEY is missing')}