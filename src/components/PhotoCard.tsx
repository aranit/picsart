import { Link } from 'react-router-dom'import { RawPexelsPhotoPreview } from '../types/photo'const PhotoCard = ({ photo }: { photo: RawPexelsPhotoPreview }) => {	return (		<Link to={`/photo/${photo.id}`} style={{ textDecoration: 'none' }}>			<div className="photo-card">				<img					src={photo.src.medium}					alt={photo.alt}					loading="lazy"					style={{ width: '100%' }}				/>			</div>		</Link>	)}export default PhotoCard