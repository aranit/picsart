import { render, screen } from '@testing-library/react'import { MemoryRouter } from 'react-router-dom'import PhotoCard from '../PhotoCard'const mockPhoto = {	id: 101,	alt: 'Beautiful landscape',	src: { medium: 'https://example.com/photo.jpg' }}describe('PhotoCard', () => {	it('renders photo with correct src and alt', () => {		render(			<MemoryRouter>				<PhotoCard photo={mockPhoto} />			</MemoryRouter>		)		const image = screen.getByRole('img')		expect(image).toHaveAttribute('src', mockPhoto.src.medium)		expect(image).toHaveAttribute('alt', mockPhoto.alt)	})})