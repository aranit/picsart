import React, { FormEvent } from 'react'interface SearchFormProps {	// Callback function to be called when a search is submitted	onSearch: (value: string) => void	query?: string}// Stateless component for the search input and submit buttonconst SearchForm = ({ onSearch, query = '' }: SearchFormProps) => {	const handleSubmit = (e: FormEvent<HTMLFormElement>) => {		e.preventDefault()		const formData = new FormData(e.currentTarget)		const value = formData.get('search')?.toString().trim()		if (value) {			onSearch(value)		}	}	return (		<form			onSubmit={handleSubmit}			style={{ marginBottom: '16px', textAlign: 'center' }}		>			<input				type="text"				name="search"				placeholder="Search photos..."				defaultValue={query}				style={{ padding: '8px', width: '250px' }}			/>			<button				type="submit"				style={{					marginLeft: '8px',					padding: '8px 16px',					cursor: 'pointer'				}}			>				Search			</button>		</form>	)}export default SearchForm