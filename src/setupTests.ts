import '@testing-library/jest-dom'// Add TextEncoder/TextDecoder polyfills for Jest environmentimport { TextEncoder, TextDecoder } from 'util'// Set global TextEncoder if not already definedif (!globalThis.TextEncoder) {	globalThis.TextEncoder = TextEncoder}// Set global TextDecoder if not already definedif (!globalThis.TextDecoder) {	globalThis.TextDecoder = TextDecoder as any}// Mock IntersectionObserver for testing components that use lazy-loading or visibility trackingclass MockIntersectionObserver {	constructor(private callback: any) {}	observe = () => this.callback([{ isIntersecting: true }])	unobserve = () => {}	disconnect = () => {}}// Use globalThis for ESM-compatible global mockingglobalThis.IntersectionObserver = MockIntersectionObserver as any// This allows your `getPexelsApiKey()` logic to work correctly in test modeprocess.env.VITE_PEXELS_API_KEY =	'uWUmcWyhZt1bTdHfNT4Lat38vgNlSgsYYIYY9ns67O6Mb0wWP3HQ36Kj'